import jwt from 'jsonwebtoken';

export const authValidator = (req, res, next) => {
    try {
        //deal with cookies and all 
        next();
        // if not 
        
    } catch(err) {

    }
}